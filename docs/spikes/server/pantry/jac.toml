[project]
name = "pantry"
version = "0.1.0"
description = "Headless Jac API service (server walkers, no client)"
entry-point = "main.sv.jac"
kind = "api-service"

[dependencies]

[dev-dependencies]
watchdog = ">=3.0.0"
