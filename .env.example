# byLLM routes through litellm. Gemini is the provider for Backshelf.
# Get a key at https://aistudio.google.com/apikey then:
#   cp .env.example .env  &&  put the real key in .env
# .env is gitignored. Never commit the real key.
GEMINI_API_KEY=
